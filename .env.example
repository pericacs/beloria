POSTGRES_DB=beloria
POSTGRES_USER=beloria
# Defina uma senha aleatória antes de subir os containers.
POSTGRES_PASSWORD=
APP_PORT=8080
# false somente para HTTP local; em servidor com HTTPS use true.
COOKIE_SECURE=false
